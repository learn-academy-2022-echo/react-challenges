
const Menu = (props) => {
    return (
     <>
    <h1>MENU</h1>
      {props.foodObj.map(currentFood,) => {
        return(

        )
      }
       }  
     </>
    );
  }














export default Menu