import React from 'react'

const Menu = ( props ) => {
    // {props.sweets.map((value, index) => {
  return (
    // <ul key={index}>
      <h1>Menu: {props.sweets.name}</h1>
     
    // </ul>
  )}
// })}
// }

export default Menu