import App from "../App";

const RobotTwo = (props) => {
    return(
        <>
        <h2>Bad Robot</h2>
<p>{props.robot2}</p>
        </>
    )
}

export default RobotTwo



//at index 0 it returns b, 1 returns l 2 returns a and so on 
//function that idetifies the index and returns the corresponding letter
